# Requirements for standalone testing
# Note: The Home Assistant integration uses HTTP API and doesn't need these

# Optional: Only needed for local testing outside Home Assistant
# moondream>=0.1.0  # Has dependency conflicts with HA, not recommended
# pillow>=10.0.0

# No dependencies required - integration uses aiohttp (built-in to Home Assistant)
